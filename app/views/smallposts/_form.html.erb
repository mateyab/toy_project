<%= form_with(model: smallpost) do |form| %>
  <% if smallpost.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(smallpost.errors.count, "error") %> prohibited this smallpost from being saved:</h2>

      <ul>
        <% smallpost.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.textarea :content %>
  </div>

  <div>
    <%= form.label :client_id, style: "display: block" %>
    <%= form.number_field :client_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
